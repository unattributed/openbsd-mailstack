# config/system.conf.example
#
# Copy this file to config/system.conf and edit it, or let the phase scripts
# prompt for missing values during execution.
#
# Public operations baseline:
# - one primary mail host identity
# - local health checks and generated operational guidance
# - no destructive automation by default
# - optional alert email target for future notifications
#
# Example:
#   MAIL_HOSTNAME="mail.example.com"
#   ADMIN_EMAIL="ops@example.com"
#   ALERT_EMAIL="ops@example.com"
#
# In that model:
# - operational summaries reference one host identity
# - checks can validate core services locally
# - alerting remains optional and conservative in MVP

OPENBSD_VERSION="7.8"
MAIL_HOSTNAME="mail.example.com"
PRIMARY_DOMAIN="example.com"
ADMIN_EMAIL="ops@example.com"
ALERT_EMAIL="ops@example.com"
PUBLIC_IPV4="203.0.113.10"
TIMEZONE="UTC"

TLS_CERT_MODE="single_hostname"
TLS_ACME_PROVIDER="acme-client"
TLS_CERT_FQDN="mail.example.com"
TLS_CERT_PATH_FULLCHAIN="/etc/ssl/mail.example.com.fullchain.pem"
TLS_CERT_PATH_KEY="/etc/ssl/private/mail.example.com.key"

ROUNDCUBE_ENABLED="yes"
ROUNDCUBE_WEB_HOSTNAME="mail.example.com"
POSTFIXADMIN_WEB_HOSTNAME="mail.example.com"
RSPAMD_UI_HOSTNAME="mail.example.com"

OPS_BACKUP_MODE="local"
OPS_RETENTION_DAYS="14"
OPS_ENABLE_ALERTS="yes"
OPS_ENABLE_HEALTHCHECKS="yes"
OPS_ENABLE_LOG_SUMMARY="yes"
