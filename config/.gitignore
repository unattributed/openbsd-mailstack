# Repo-local operator inputs
system.conf
network.conf
domains.conf
secrets.conf
backup.conf
backup-schedule.conf
dr-site.conf
dr-host.conf
monitoring.conf

# Local overlays and provider credentials
local/
*.local
*.bak
