# SBOM

This directory holds public-safe SBOM and host inventory assets.

Included here:

- `components/cpe-map.tsv`
- `components/manual-components.tsv`
- `exceptions/exceptions.tsv.template`
- runtime report ignore rules

The public workflow generates reports under `services/generated/sbom/`.
