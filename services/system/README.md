# System overlays

This directory holds public-safe system overlays that are directly related to the public operational baseline.

For Phase 05 the primary system overlay is the monitoring newsyslog managed block, which rotates the monitoring run log and reporting log without assuming the private host layout.
