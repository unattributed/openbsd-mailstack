# DNS Assets

This directory contains public-safe DNS assets for:

- Unbound split-DNS rendering
- DNS identity guidance files generated by Phase 09

Live provider records and private zone exports are intentionally excluded.
